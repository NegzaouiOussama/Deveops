apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: devops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Note: Pour Minikube, le storage-provisioner gère automatiquement les PVs
  # Si vous utilisez hostPath manuellement, retirez storageClassName
  # storageClassName: ""

